/*
    Запустить команду — `tsc --build ./9`
    указать свойству declaration значение — true или false
    это свойство определяет необходимость генераци d.ts файлов
*/

import {sum} from "./index1";
import {multiply} from "./index2";
